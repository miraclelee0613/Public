<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.springdream.app.mapper.BoardMapper">
<<<<<<< HEAD
    <!--  게시글 추가  -->
    <insert id="insert" parameterType="boardVO">
        <selectKey keyProperty="boardNumber" order="BEFORE" resultType="long">
            SELECT SEQ_BOARD.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_BOARD(BOARD_NUMBER, BOARD_DATE, BOARD_UPDATE_DATE, BOARD_TITLE, BOARD_CONTENT,
        BOARD_REWARD, BOARD_CATEGORY, BOARD_ADOPT, BOARD_REPORT, MEMBER_NUMBER)
        VALUES(#{boardNumber}, SYSDATE, SYSDATE, #{boardTitle}, #{boardContent},
        #{boardReward}, #{boardCategory}, #{boardAdopt}, #{boardReport}, #{memberNumber})
    </insert>
    <!--  게시글 수정  -->
    <update id="update">
        UPDATE TBL_BOARD SET
        BOARD_UPDATE_DATE = SYSDATE, BOARD_TITLE = #{boardTitle}, BOARD_CONTENT = #{boardContent},
        BOARD_REWARD = #{boardReward}, BOARD_CATEGORY = #{boardCategory}, BOARD_ADOPT = #{boardAdopt},
        BOARD_REPORT = #{boardReport}, MEMBER_NUMBER = #{memberNumber}
        WHERE BOARD_NUMBER = #{boardNumber}
    </update>
    <!--  게시글 삭제  -->
=======
<!-- 게시글 추가 -->
<!--  keyProperty에 boardNumber를 가져와도 value값에서 똑같이 쓰는게 아니다  -->
<!--  작성한 VO값에 덮어씌워지고 다시 불러와 insert문의 boardNumber값에 들어간다.  -->
    <insert id="insert">
        <selectKey keyProperty="boardNumber" order="BEFORE" resultType="long">
            SELECT SEQ_BOARD.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_BOARD(BOARD_NUMBER, BOARD_TITLE, BOARD_CONTENT, BOARD_REWARD, BOARD_CATEGORY)
        VALUES(#{boardNumber}, #{boardTitle}, #{boardContent}, #{boardReward}, #{boardCategory})
    </insert>

<!--  게시글 수정  -->
    <update id="update">
        UPDATE TBL_BOARD
        SET BOARD_TITLE = #{boardTitle}, BOARD_CONTENT = #{boardContent}, BOARD_UPDATE_DATE = SYSDATE
        WHERE BOARD_NUMBER = #{boardNumber}
    </update>

<!--  게시글 삭제  -->
>>>>>>> 96b6b6afb51683d395c065a2d524b8e3d8cb1165
    <delete id="delete">
        DELETE FROM TBL_BOARD
        WHERE BOARD_NUMBER = #{boardNumber}
    </delete>
<<<<<<< HEAD
    <!--  게시글 전체조회  -->
    <select id="selectAll" resultType="boardVO">
        SELECT * FROM TBL_BOARD
    </select>
    <!--  게시글 조회  -->
    <select id="select" resultType="boardVO">
        SELECT * FROM TBL_BOARD WHERE BOARD_NUMBER = #{boardNumber}
    </select>
    <!--  게시글 전체갯수  -->
    <select id="getTotal" resultType="_int">
        SELECT COUNT(BOARD_NUMBER) FROM TBL_BOARD
=======

<!--  조회  -->
    <select id="select" resultType="boardDTO">
        SELECT BOARD_NUMBER, BOARD_DATE, BOARD_UPDATE_DATE, BOARD_TITLE, BOARD_CONTENT, BOARD_REWARD, BOARD_CATEGORY
        FROM TBL_BOARD
        WHERE BOARD_NUMBER = #{boardNumber}
    </select>

<!--  게시글 전체 조회  -->
<!--  썸네일 방식을 몰라 잠시 보류  -->
    <select id="selectAll" resultType="boardDTO">
        SELECT BOARD_NUMBER, BOARD_DATE, BOARD_UPDATE_DATE, BOARD_TITLE, BOARD_CONTENT, BOARD_REWARD, BOARD_CATEGORY
        FROM TBL_BOARD
>>>>>>> 96b6b6afb51683d395c065a2d524b8e3d8cb1165
    </select>
</mapper>

















